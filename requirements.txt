# Semantica 인제스천 파이프라인 의존성
# 설치: pip install -r requirements.txt

# ── 핵심 프레임워크 ───────────────────────────────────────────────────────────
semantica==0.6.6

# ── Vertex AI / Claude ───────────────────────────────────────────────────────
anthropic[vertex]>=0.40.0

# ── 임베딩 (Vertex AI Text Embedding API — 로컬 모델 불필요) ─────────────────
google-cloud-aiplatform>=1.38.0

# ── 벡터 스토어 ──────────────────────────────────────────────────────────────
qdrant-client>=1.9.0

# ── 그래프 스토어 ─────────────────────────────────────────────────────────────
redis>=5.0.0          # FalkorDB 클라이언트 (Redis 호환)
falkordb>=1.0.0       # FalkorDB Python 드라이버

# ── Notion API ────────────────────────────────────────────────────────────────
httpx>=0.24.0

# ── MCP 서버 (Phase 2) ───────────────────────────────────────────────────────
fastmcp>=0.1.0
