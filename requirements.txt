# Semantica 인제스천 파이프라인 의존성
# 설치:
#   pip install semantica==0.6.6 --no-deps
#   pip install -r requirements.txt

# ── Vertex AI / Claude ────────────────────────────────────────────────────────
anthropic[vertex]>=0.40.0
google-genai>=1.0.0               # google.genai (Vertex AI 통합 SDK)
google-cloud-aiplatform>=1.38.0   # Text Embedding API (torch 불필요)

# ── 벡터 스토어 ───────────────────────────────────────────────────────────────
qdrant-client>=1.9.0

# ── 그래프 스토어 ─────────────────────────────────────────────────────────────
falkordb>=1.0.0
redis>=5.0.0

# ── MCP 서버 ──────────────────────────────────────────────────────────────────
fastmcp>=2.0.0

# ── Semantica 프레임워크 (엔티티 중복 제거 / NER·RE fallback) ─────────────────
# graph-falkordb 번들: FalkorDB 백엔드 + NER/RE 포함
semantica[graph-falkordb]

# ── 운영 로그 (PostgreSQL) ────────────────────────────────────────────────────
psycopg2-binary>=2.9.0

# ── NLP / 엔티티 추출 ────────────────────────────────────────────────────────
spacy>=3.7.0                      # Semantica NER/RE ML fallback 필수
# spaCy 언어 모델은 pip install 후 아래 명령으로 별도 설치:
#   python -m spacy download en_core_web_sm      # 영문
#   python -m spacy download ko_core_news_sm     # 한국어

# ── 유틸 ──────────────────────────────────────────────────────────────────────
httpx>=0.24.0
numpy>=1.24.0
pyyaml>=6.0.0                     # departments.yaml 파싱
