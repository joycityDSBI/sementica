# Design: 조이시티 온톨로지 지식 인프라 (Notion → Semantica → MCP)

Generated by /office-hours on 2026-08-28
Branch: unknown
Repo: c:/sementica (local, non-git)
Status: APPROVED (2026-08-28)
Mode: Startup (Intrapreneurship)

## Problem Statement

조이시티 여러 팀(개발, 기획, HR 등)이 Notion에 축적된 회사 지식을 효율적으로 조회하지 못하고 있다. 담당자 확인, 정책 조회, KPI 맥락 파악 등 업무 필수 정보를 찾을 때마다 Notion 검색 + 수동 탐색에 의존한다. 관계형 질의("A의 담당자는?", "이 규정은 누가 관리해?")가 불가능하고, 여러 페이지에 흩어진 정보를 종합하는 데 추가 시간이 소요된다.

목표: Notion 페이지를 자동으로 수집해 Semantica 기반 온톨로지(지식 그래프 + 벡터 스토어)로 구조화하고, Claude Code MCP를 통해 자연어로 질의하는 환경을 구축한다.

## Demand Evidence

- **빌더 본인이 제1 유저**: 사라져도 가장 파손되는 사람이 본인. 실제 필요로 만드는 도구.
- **여러 팀에서 동시 발생**: 개발팀만의 문제가 아닌 조직 전반의 정보 탐색 문제. 임원 승인 논거 가능.
- **현재 대안 부재**: Notion AI Q&A가 있으나 관계형 질의 불가. 온톨로지 구조 없음. 의미 검색 한계.

## Status Quo

직원이 "X 담당자가 누구야?" 혹은 "Y 정책 내용이 뭐야?" 같은 질문을 받으면:
1. Notion 검색창에 키워드 입력
2. 결과 페이지 수동 탐색 (복수 페이지에 분산 시 반복)
3. 정보가 없거나 오래된 경우 Slack에서 동료에게 직접 질문

**비용**: 단순 조회 1건당 10-30분 이상 소요 추정. 정보 최신성 보장 불가.

## Target User & Narrowest Wedge

**1차 타겟**: Claude Code를 사용하는 개발/기획 팀원 (기술 친숙도 있는 직원)
**2차 타겟**: Slack Bot 등 비기술적 인터페이스 추가 후 전사 확장

**최소 데모 (Week 2 목표)**: 인사 관련 Notion 페이지 20-30개를 Semantica에 적재한 뒤, Claude Code에서 "X팀 온보딩 담당자는?"에 답변이 나오는 것을 팀장에게 시연.

## Constraints

- Semantica가 신생 라이브러리 (0.x 버전 수준). 자동 추출 품질 미검증.
- Claude Code는 개발자 인터페이스. 비개발팀 접근성 문제.
- Notion API Rate limit 및 페이지 구조 다양성 (structured vs free-form).
- 단독 빌더 기준 12주 일정.

## Premises

1. 조이시티 Notion 페이지들이 온톨로지 추출 원본으로 충분한 품질을 갖추고 있다. **합격 기준**: Week 1 샘플 10페이지 중 6페이지 이상에서 의미 있는 텍스트 블록 추출 가능. 실패 시 구조화된 Notion DB 페이지 우선 적재로 전환.
2. 1차 타겟은 Claude Code를 사용하는 개발/기획 팀이다. 비기술직은 12주 범위 밖 (Phase 4+ Slack Bot 이후).
3. Semantica가 수동 스키마 설계 없이 유의미한 그래프를 자동 구축할 수 있다. **합격 기준**: 10페이지 샘플에서 6페이지 이상에 3개 이상의 엔티티-관계-엔티티 트리플 자동 추출. 실패 시 수동 스키마 설계로 전환 (Phase 1에 1주 추가).
4. 팀장/본부장 레벨 승인은 Week 2 데모 한 번으로 가능하다. 추가 stakeholder 검토가 필요할 경우 개인 권한으로 Phase 2를 시작하며 최대 1주 지연 허용.

## Approaches Considered

### Approach A: RAG 우선 PoC (벡터만, 그래프 없음) — 기각 (Week 1 실패 시 Fallback)
- 1-2주 안에 배포 가능하나 관계형 질의 불가.
- "담당자는 누구?" 같은 핵심 use case를 구조적으로 커버하지 못함. 의미 검색은 되지만 그래프 관계 추론 불가.
- **기각 이유**: Approach B의 핵심 가치(관계형 질의)를 포기하게 된다. 단, Week 1 Semantica 검증 실패 시 Approach A(벡터 단독)로 전환해 Week 2 데모를 진행하고 그래프 아키텍처를 재설계한다.

### Approach B: Ontology MCP 집중 (벡터+그래프, 12주) — **채택**
- Notion → Semantica (Vector + Graph) → Ontology MCP 단일 구현으로 핵심 문제 해결.
- NL-SQL/KPI MCP는 별도 프로젝트로 분리해 스코프 현실화.
- **채택 이유**: 관계형 질의와 의미 검색을 모두 커버하면서 단독 빌더 12주 일정에 맞는 유일한 경로.

### Approach C: Slack Bot 우선 — Phase 4로 연기
- 5-6주로 전사 접근성 확보 가능하나 Claude Code MCP 통합 깊이 희생.
- **연기 이유**: Ontology MCP 안정화 이후 별도 Phase로 추가. 12주 범위 밖.

## Recommended Approach

**Approach B: Ontology MCP 집중 아키텍처 (12주)**

```
Notion API → 수집 파이프라인
     │
     ▼
Semantica (Vector Store + Property Graph)
  - LLM 기반 자동 온톨로지 추출 (엔티티, 관계, 동의어)
  - Qdrant (벡터, Docker self-hosted) + FalkorDB (그래프, Docker open-source)
     │
     ▼
Ontology MCP 서버 (FastMCP, Phase 1: 개발자 로컬 Docker → Phase 2: 사내 서버)
  - semantic_search: 의미 유사도 검색 (벡터)
  - graph_search:   엔티티 관계 탐색 (그래프)
  - hybrid_search:  벡터 + 그래프 복합 질의
     │
     ▼
Claude Code (.claude/settings.json에 MCP 등록)
  ↓ 자연어 질의
  ↓ LLM 오케스트레이션 (Tool Description 기반 MCP 자동 선택)
  ↓ MCP Proxy: 쿼리에서 동의어 감지 → 정규화된 엔티티명으로 치환 후 MCP 전달
  ↓ 근거 포함 한국어 답변
```

**Phase 1 (1-4주)**: Notion 수집 파이프라인 + Semantica 적재 + Ontology MCP 단일 구현. Semantica 추출 품질 Week 1 합격 기준 통과 확인. Notion API Rate Limit(3 req/s) 준수하는 배치 수집기 구현. 수집 실패 시 로컬 로그 기록 후 단순 재시도(최대 3회). **Phase 1 인제스트 시 모든 엔티티/청크에 `source_url` (Notion 페이지 URL) 필드 저장 — MCP 응답에 출처 URL 포함은 Week 2 성공 기준.** Phase 3의 감사 로그는 이 source_url을 쿼리 가능한 로그로 구조화하는 추가 작업.

**Phase 2 (5-8주)**: 동의어 DB(SQLite) 구축 + MCP Proxy 레이어(FastMCP 미들웨어로 쿼리 전처리, 동의어 치환 후 Ontology MCP로 전달). CLAUDE.md 오케스트레이션 규칙. Notion 페이지 변경 감지(Last-Edited-Time 폴링, 6시간 주기) 및 증분 업데이트. Dead-letter queue 구현(수집 실패 페이지 재처리). Semantica 장애 시 Fallback: 단순 Notion API 키워드 검색으로 자동 전환, 사용자에게 "정확도 저하 모드" 안내.

**Phase 3 (9-12주)**: 동의어 DB → Semantica 온톨로지 마이그레이션(동의어를 그래프 노드로 흡수). 소스 Notion 페이지 URL 감사 로그(페이지별 source_url 필드). 평가 지표 측정(golden set 정밀도/재현율).

**Phase 4+ (선택적 확장, 별도 프로젝트)**: Slack Bot 연동. NL-SQL MCP (BigQuery Golden Layer 자연어 쿼리) + KPI MCP (KPI 정의·달성률·트렌드). 각각 독립 프로젝트 규모로 설계 필요.

## Open Questions

1. Semantica 자동 추출이 조이시티 Notion 페이지 구조에서 어떤 품질을 낼지 Week 1에 실증 필요.
2. **LLM 비용 추정**:
   - 초기 적재: 1,000페이지 × 2,000토큰 = 200만 토큰 → Claude Haiku 기준 약 $0.25
   - 운영(증분 재인덱싱, 6시간 주기): 하루 변경 페이지 ~50개 × 2,000토큰 × 4회 = 40만 토큰/일 → 월 약 $3.75
   - 쿼리(사용자 질의): 쿼리당 ~500토큰 × 팀 합산 50쿼리/일 = 2.5만 토큰/일 → 월 약 $0.94
   - **합계: 초기 $0.25 + 월 ~$5 내외.** 예산 승인 필요 없는 수준이나 초기 적재 승인 항목으로 포함.
3. Notion 팀스페이스별 접근 권한 관리: 통합 읽기 전용 Integration 계정 사용. 민감 HR 데이터 페이지는 Notion에서 Integration 접근 제외(팀스페이스 권한 설정)로 MCP 레벨 필터링 불필요.
4. 동의어 DB 초기 seed 데이터를 어느 팀과 함께 정의할지 (HR? 데이터팀?).

## Success Criteria

- **Week 2**: 20문항 골든셋(Week 1 샘플 페이지 기반) 중 15/20 정답 + 출처 Notion URL 반환. 팀장 시연 통과.
- **Week 8**: hybrid_search로 복합 질의(담당자 + 관련 문서) → 10초 P95 이내 답변 (초기 버전 목표; Phase 3에서 5초 이하 목표). 골든셋 18/20 이상.
- **Week 12**: 개발팀·기획팀 각 최소 3명의 활성 사용자가 2주 연속 주 5쿼리 이상 사용. Claude Code 기반 팀 2개 정착 확인. (비기술직 확장은 Phase 4+ Slack Bot 이후 별도 지표)

## Distribution Plan

- Claude Code `.claude/settings.json` MCP 등록으로 팀 내 배포 (설정 파일 공유)
- MCP 서버: Phase 1은 개발자 로컬 Docker, Phase 2부터 사내 공용 서버 이전
- Phase 4+ (12주 범위 밖): Slack Bot으로 비기술직 접근 확대

## Dependencies

- Notion API 접근 권한 (통합 읽기 전용 Integration 계정)
- Semantica `pip install semantica-agi` 및 Python 3.10+ 환경
- Docker (Qdrant + FalkorDB 컨테이너 실행)
- Claude Code 설치된 팀원 환경

## The Assignment

**이번 주에 할 것 (Week 1 검증)**:
조이시티 Notion에서 인사/온보딩 관련 페이지 10개를 골라 Semantica에 수동으로 적재(`semantica ingest`)하고, 나온 그래프를 직접 확인하라.

**합격 판정 (두 조건 모두 충족해야 Phase 2 진입)**:
- Premise 1: 10페이지 중 6페이지 이상에서 의미 있는 텍스트 추출 성공
- Premise 3: 10페이지 중 6페이지 이상에서 3개 이상의 엔티티-관계-엔티티 트리플 자동 추출 (예: "박지수 → 담당 → 채용공고")

**실패 시**: Premise 1 실패 → 구조화된 Notion DB 페이지 우선 적재로 전환. Premise 3 실패 → 수동 스키마 설계 경로로 전환 (1주 추가). 두 경우 모두 Week 2 데모는 벡터 단독(Approach A 폴백)으로 진행 가능.

이 검증 없이 Week 2로 넘어가면 안 된다.

**병행 작업 (Week 1 중 제출)**:
- IT에 사내 공용 서버 Docker 환경 신청 (Qdrant 포트 + FalkorDB 포트). IT 승인 2-4주 소요 가능 — Phase 2 서버 이전 전에 신청하지 않으면 Phase 2가 지연된다.
- 20문항 골든셋을 인제스트 **전**에 작성하라 (시스템이 이미 답할 수 있는 질문을 골든셋으로 삼는 편향 방지). 알고 있는 사람에게 "당신이 가장 자주 묻는 온보딩 질문 20개"를 수집해도 된다.

## What I noticed about how you think

- 대화 초반부터 "vector DB와 graph DB를 LLM은 어떻게 사용하나"를 묻는 사람은 드물다. 단순히 "뭘 쓸지"가 아니라 "LLM이 어떻게 판단하는지"를 이해하려 했다. 아키텍처를 레이어별로 이해한 후 설계하는 방식.
- A로 시작하라는 권고를 거부하고 B 전체를 선택했다. "빠른 증명"보다 "완성된 아키텍처"를 선호하는 확신. 이 확신이 맞으려면 Week 1 검증이 필수다.
- Notion 데이터 → Semantica로 가는 파이프라인을 논의할 때 "동의어 문제"를 본인이 먼저 꺼냈다. 실제 운영에서 나오는 문제를 이미 예상하고 있다는 것. 이론이 아닌 실운영 감각.
- 이 시스템을 "나 혼자를 위한 도구"로 시작하면서도 "다른 팀도 같은 문제"라는 것을 즉각 인식했다. 좋은 내부 도구가 어떻게 회사 인프라가 되는지 알고 있는 사람.
